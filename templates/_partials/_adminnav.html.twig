<aside class="col-3 border border-light bg-white p-2">
    <article class="card mb-3">
        <div class="card-header">
            Catégories
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_categorie_index') }}">Liste des catégories</a></p>
            <p><a href="">Ajouter une catégorie</a></p>
        </div>
    </article>
    <article class="card mb-3">
        <div class="card-header">
            plats
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_plat_index') }}">Liste des plats</a></p>
            <p><a href="{{ path('admin_plat_add') }}">Ajouter un plat</a></p>
        </div>
    </article>
    <article class="card mb-3">
        <div class="card-header">
            Utilisateurs
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_utilisateur_index') }}">Liste des utilisateurs</a></p>
        </div>
    </article>
</aside>